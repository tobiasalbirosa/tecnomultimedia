void setup() {
  size(400, 400);
  frameRate(12);
  ellipseMode(CENTER);
}
void draw() {
  miBotonCircular(width/4, height/2, width/10, 0);
  miBotonCircular(width/2, height/2, width/10, 1);
}
